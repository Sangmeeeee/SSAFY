# Info

문제 : [공항](https://www.acmicpc.net/problem/10775)

알고리즘 종류 : 분리 집합

참조 url : 


# 1. 알고리즘 설명

1. `root` 배열을 자기 자신으로 초기화한다.
2. 입력받은 `g`에 대해 `find()`를 통해 루트를 찾는다.
	- 만약 `g`의 루트 노드가 0이라면 더 이상 도킹할 수 없다는 뜻이므로 break 한다.
	- 그렇지 않다면 `g`의 루트 노드와 `g`의 루트 노트보다 1만큼 작은 노드를 `Union()`을 통해 합친다.
	- `answer` 변수를 1만큼 증가시킨다.


# 2. 새로 배운 내용(코드 위주)